# Backend environment variables (copy to .env and fill values)
# Database
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=''
DB_NAME=wrsha1
DB_CONNECTION_LIMIT=10

# App server
PORT=4001
HOST=localhost

# Auth / secrets
JWT_SECRET=replace_with_a_secure_random_value

# Test helper (optional) - token used by checkLogout script
AUTH_TEST_TOKEN=your_test_jwt_here
BACKEND_BASE_URL=http://localhost:4001